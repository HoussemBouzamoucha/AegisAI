# ============================================================
# Malware Memory-Forensics — Application Requirements
# ============================================================
# Install with: pip install -r requirements.txt
#
# Core dependencies (required for preprocess.py + inference.py)
# ---------------------------------------------------------------
numpy>=1.24,<3.0
pandas>=1.5,<3.0
scikit-learn>=1.3,<2.0
joblib>=1.3

# Gradient-boosting models
# ---------------------------------------------------------------
xgboost>=2.0
lightgbm>=4.0

# Parquet support (used by fit_and_save and predict_file)
# ---------------------------------------------------------------
pyarrow>=13.0          # preferred parquet engine
# fastparquet>=2023.10  # alternative — uncomment if you prefer it

# ============================================================
# Optional / situational
# ============================================================

# SHAP explanations (used in train.py diagnostics; not needed at inference time)
# shap>=0.44

# ONNX inference  (needed only if you export models to .onnx format)
# onnxruntime>=1.17
# skl2onnx>=1.16        # convert sklearn models to ONNX
# onnxmltools>=1.12     # convert xgb/lgb models to ONNX

# Jupyter / Google Colab (development only)
# ipykernel>=6.0
# notebook>=7.0

# Plotting (used in train.py and diagnostics.py; not needed at inference time)
# matplotlib>=3.7
# scipy>=1.11            # used in diagnostics.py for ks_2samp